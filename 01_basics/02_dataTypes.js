"use strict";  //treat all JS code as new version

// alert('hello') //we are using nodejs, not browser

//code readability should be high

//number => 2^53
//bigint
// string => ""
// boolean => true/false
// null => standalone value -- typeof-object
// undefined => (ex. let state)value is not assigned yet typeof-undefined
// symbol => unique(figma used)

// object

console.log(typeof 'sneha')
